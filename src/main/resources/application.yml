spring:
  application:
    name: ReciGuard
  jwt:
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb
  datasource:
    url: jdbc:mysql://localhost:3306/RECIGUARD?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: danchoo12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true
  logging:
    level:
      org.hibernate.stat: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: OFF
  sql:
    init:
      data-locations: classpath:db/data.sql
      mode: always
      platform: sql
  cloud:
    aws:
      S3:
        bucket: reciguard-bucket
        stack.auto: false
        region.static: ap-northeast-2
        credentials:
          accessKey: AKIA3ISBV7TLKQQJGZDL
          secretKey: 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
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB